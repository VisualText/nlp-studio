node_modules/
dist/
out/
*.log

# Built extension package -- produced by stopgap/scripts/build-vsix.*, not checked in
# (it is ~10 MB and rebuilt from vscode-nlp on demand).
*.vsix
stopgap/vsix/

# The studio's analyzers as the page fetches them -- written by
# studio/scripts/copy-samples.mjs from studio/samples/ and a sibling
# analyzer-templates checkout, which has no license to commit it under.
studio/public/analyzers/

# The try page's analyzers as it fetches them -- written by
# studio/scripts/copy-analyzers.mjs at build time from a VisualText/analyzers
# release. Build output, and a second copy of another repository besides.
studio/public/try/

__pycache__/
.venv/

.DS_Store
Thumbs.db

# Runtime state from stopgap/scripts/update-studio.sh
stopgap/logs/
stopgap/.update.lock
studio/deploy/.update.lock
# The app's sign-in settings, secret included: written on the server (studio/deploy/env.example).
studio/deploy/.env
