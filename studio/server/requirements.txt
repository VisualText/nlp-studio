# The run server is standard-library Python; the engine is its one dependency.
#
# Pinned. 2.2.38 is the first with nlp-engine 4.1.3, whose openfile() creates the
# file it names on Linux (VisualText/nlp-engine#742); 2.2.37 and earlier wrote
# nothing there. readfile() and callconcept() arrived after 2.2.8 and 2.2.23, and
# the analyzers and templates the studio opens use them.
NLPPlus==2.2.38
